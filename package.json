{
  "name": "project",
  "version": "1.0.0",
  "description": "Project: server + client",
  "scripts": {
    "build": "docker compose up --build",
    "start": "docker compose up",
    "stop": "docker compose down"
  }
}